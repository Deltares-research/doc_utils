# `ddocs.repo_cloner`

Clone a git repository into a temporary directory and copy files out of it,
with optional HTTPS authentication. Used to fetch the Deltares
`LatexInstallation` templates for the `get-tex-template` command.

::: ddocs.repo_cloner
    options:
      show_root_heading: true
      show_source: true
